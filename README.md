# Weald Updates

Version manifest for the Weald macOS app's built-in update check.
The app fetches `version.json` and compares `version` against the running build.

- `version`  — latest released version string (matches CFBundleShortVersionString)
- `notes`    — short release notes shown in the update prompt
- `download` — where to get the new build
